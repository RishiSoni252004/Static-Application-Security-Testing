# Static-Application-Security-Testing

A basic Static Application Security Testing (SAST) CLI tool built with Node.js.

## Features
- Regex-based vulnerability scanning
- Modular rule engine
- CLI reporting

## Usage
```bash
node index.js --dir <directory_to_scan>
```
